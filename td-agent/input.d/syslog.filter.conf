<filter system.**>
    @type record_transformer
    <record>
        facility ${tag}
    </record>
</filter>

<filter system.**>
    @type grep
    <regexp>
        key facility
        pattern /\.notice$/
    </regexp>
</filter>

